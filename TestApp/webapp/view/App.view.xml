<mvc:View
        displayBlock="true"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns:core="sap.ui.core"
        xmlns:l="sap.ui.layout"
        xmlns:f="sap.ui.layout.form"
        xmlns="sap.m"
        controllerName="projectone.TestApp.controller.App">

    <App>
        <pages>
            <Page title="Raktárkészlet átkönyvelés">
                <IconTabBar
                        id="iconTabBar"
                        expandable="false"
                        applyContentPadding="false">
                    <items>
                        <IconTabFilter
                                icon="sap-icon://toaster-up"
                                iconColor="Neutral"
                                text="{i18n>plantListTitle}">
                            <content>
                                <List
                                        headerText="{i18n>plantListHeader}"
                                        items="{
							path:'/PlantSet'}">
                                    <items>
                                        <ObjectListItem
                                                title="{Name}"/>
                                    </items>
                                </List>
                            </content>
                        </IconTabFilter>
                        <IconTabFilter
                                icon="sap-icon://product"
                                iconColor="Neutral"
                                text="{i18n>materialListTitle}">
                            <content>
                                <List
                                        headerText="{i18n>materialListHeader}"
                                        items="{
							path:'/MaterialSet'}">
                                    <items>
                                        <ObjectListItem
                                                title="{Name}"/>
                                    </items>
                                </List>
                            </content>
                        </IconTabFilter>
                        <IconTabFilter
                                icon="sap-icon://toaster-down"
                                iconColor="Neutral"
                                text="{i18n>plantToListTitle}"/>
                        <IconTabFilter
                                icon="sap-icon://show-edit"
                                iconColor="Positive"
                                text="{i18n>transferpostingListTitle}"/>
                    </items>
                </IconTabBar>

            </Page>
        </pages>
    </App>
</mvc:View>